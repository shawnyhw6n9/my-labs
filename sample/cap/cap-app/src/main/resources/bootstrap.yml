spring:
  cloud:
    config:
      name: config
      profile: dev 
      uri: http://cloudconfig-server-1:8001/config-server,http://localhost:8003/config-server
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://cloudconfig-server1:8001/eureka,http://cloudconfig-server1:8003/eureka